<UserControl x:Class="NoteOrganizer.Views.SchedulerDayHeaderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             Background="#1f1f1f"
             d:DesignHeight="75" d:DesignWidth="800">
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="35" />
      <RowDefinition Height="30" />
    </Grid.RowDefinitions>
    <Label Content="{Binding DayOfMonth}" Foreground="White" FontWeight="Bold" FontSize="20" />
    <Label Content="{Binding Weekday}" Foreground="White" Grid.Row="1" />
  </Grid>
</UserControl>