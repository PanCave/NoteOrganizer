<UserControl x:Class="NoteOrganizer.Views.SchedulerWeekView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:view="clr-namespace:NoteOrganizer.Views"
             mc:Ignorable="d"
             d:DesignHeight="1450" d:DesignWidth="1000">
  <Grid>
    <Grid.ColumnDefinitions>
      <ColumnDefinition />
      <ColumnDefinition />
      <ColumnDefinition />
      <ColumnDefinition />
      <ColumnDefinition />
    </Grid.ColumnDefinitions>
    <view:SchedulerDayView Grid.Column="0" DataContext="{Binding MondayViewModel}" />
    <view:SchedulerDayView Grid.Column="1" DataContext="{Binding TuesdayViewModel}" />
    <view:SchedulerDayView Grid.Column="2" DataContext="{Binding WednesdayViewModel}" />
    <view:SchedulerDayView Grid.Column="3" DataContext="{Binding ThursdayViewModel}" />
    <view:SchedulerDayView Grid.Column="4" DataContext="{Binding FridayViewModel}" />
  </Grid>
</UserControl>