<Window x:Class="NoteOrganizer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:views="clr-namespace:NoteOrganizer.Views;assembly=NoteOrganizer.Views"
        mc:Ignorable="d"
        Background="#0a0a0a"
        Title="MainWindow" Height="1080" Width="1920">
  <Grid>
    <!--<views:SchedulerWeekView DataContext="{Binding SchedulerWeekViewModel}" />-->
    <!--<views:TodoListView DataContext="{Binding TodoListViewModel}" />-->
    <views:CombinedView DataContext="{Binding CombinedViewModel}" />
  </Grid>
</Window>